version: '3.8'

services:
  golang-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - .:/root/ocpp/cs_ocpp2.0/
    environment:
      - ENV=production
    restart: unless-stopped